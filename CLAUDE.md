# Instrucciones del proyecto

- Este repo usa un proyecto de Supabase en producción (`ijkthelpxsyjicanduaj`). El usuario autorizó a Claude a hacer cambios ahí sin pedir confirmación cada vez: crear tablas, migraciones, políticas RLS, corregir/migrar datos existentes, etc. No hace falta preguntar antes de aplicar una migración razonable y relacionada con lo que se está pidiendo.
- Sí hay que avisar (sin bloquear el trabajo) si se detecta algo riesgoso no relacionado con el pedido actual, como una tabla sin RLS habilitado.
